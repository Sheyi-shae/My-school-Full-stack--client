import React from 'react';
import './Preloader.css'; // Make sure to import your CSS file

const Preloader = () => {
  return (
    <div className="preloader-container">
      <div className="preloader"></div>
    </div>
  );
};

export default Preloader;
